<div id="<%= dom_id user %>">
  <p>
    <strong>Name:</strong>
    <%= user.name %>
  </p>

  <p>
    <strong>Email:</strong>
    <%= user.email %>
  </p>

  <p>
    <strong>Campaigns list:</strong>
    <%= user.campaigns_list.map { |ele| ele['campaign_name']}.join(',')%>
  </p>

</div>
